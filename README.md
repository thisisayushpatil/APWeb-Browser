# APWeb-Browser
A minimal, privacy‑friendly Windows desktop browser built with the Microsoft Edge WebView2 control. It uses DuckDuckGo as the default search engine and focuses on clean UI, fast startup, and sensible defaults.
